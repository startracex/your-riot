<static-base-path>
  <routes base="/app">
    <a href="/home">
      Home
    </a>
    <route path="(.*)">
        <p>{state.message}</p>
    </route>
  </routes>

  <script>
    import { Routes, Route } from '../../dist/module/index.js'

    export default {
      components: {
        Routes,
        Route,
      },
      state: {
        message: 'hello',
      }
    }
  </script>
</static-base-path>
