<same-route-matches>
  <routes>
    <route path="(.*)">
      <p>{state.message}</p>
    </route>
  </routes>

  <script>
    import { Routes, Route } from '../../dist/module/index.js'

    export default {
      components: {
        Routes,
        Route,
      },
      state: {
        message: 'hello',
      }
    }
  </script>
</same-route-matches>
