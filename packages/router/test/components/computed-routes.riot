<computed-routes>
  <routes>
    <route path={state.home}>
      <p>{state.name}</p>
    </route>
  </routes>
  <script>
    import { Routes, Route } from '../../dist/module/index.js'

    export default {
      components: {
        Routes,
        Route,
      },
      state: {
        home: '/home',
        name: 'hello'
      },
    }
  </script>
</computed-routes>
