<computed-routes>
  <router>
    <route path={state.home}>
      <p>{state.name}</p>
    </route>
  </router>
  <script>
    import { Router, Route } from '../../dist/module/index.js'

    export default {
      components: {
        Router,
        Route,
      },
      state: {
        home: '/home',
        name: 'hello'
      },
    }
  </script>
</computed-routes>
